import * as instance from '../src/fjlReCaptchaValidator';

describe ('fjl-validator-recaptcha', function () {
    test ('should have reached this point with no errors', function () {
        expect(!!instance).toEqual(true);
    });
});
